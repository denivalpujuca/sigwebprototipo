# Build do front (Vite). Em produção no Cloudflare Pages, defina a mesma variável nas configurações do projeto.
# URL do Worker (Hono + D1), sem barra no final.
VITE_API_URL=https://sigweb-api.seu-subdominio.workers.dev
