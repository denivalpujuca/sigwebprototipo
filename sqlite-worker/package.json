{
  "name": "sigweb-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "npm install --prefix sigweb-api && npm run deploy --prefix sigweb-api",
    "dev": "npm run dev --prefix sigweb-api"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "hono": "^4.6.19"
  },
  "devDependencies": {
    "wrangler": "^4.81.1"
  }
}
